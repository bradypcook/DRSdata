 // import type { NextConfig } from "next"; - ARCHIVED

const repo = "DRSdata";

const nextConfig = {
  output: 'export',
  basePath: `/${repo}`,
  assetPrefix: `/${repo}/`,
};

export default nextConfig;
